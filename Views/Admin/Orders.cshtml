@model IEnumerable<RestaurantERP.Models.Order>
@{
    ViewData["Title"] = "Orders";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var completed = Model.Where(o => o.Status == RestaurantERP.Models.OrderStatus.Completed).ToList();
    var pending = Model.Where(o => o.Status == RestaurantERP.Models.OrderStatus.Pending || o.Status == RestaurantERP.Models.OrderStatus.Preparing).ToList();
}

<div class="page-header">
    <div>
        <h1 class="page-title" data-t="orders">Orders</h1>
        <p class="page-subtitle">@Model.Count() <span data-t="totalOrders">total orders</span></p>
    </div>
    <div class="d-flex gap-2">
        <input type="date" id="dateFilter" class="form-control" value="@DateTime.Today.ToString("yyyy-MM-dd")" onchange="applyFilters()">
    </div>
</div>

<div class="stats-grid" style="grid-template-columns:repeat(4,1fr);margin-bottom:24px">
    <div class="stat-card">
        <div class="stat-icon" style="background:#45B7D122;color:#45B7D1"><i class="fas fa-receipt"></i></div>
        <div class="stat-value">@Model.Count()</div>
        <div class="stat-label" data-t="totalOrders">Total Orders</div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:#4CAF5022;color:#4CAF50"><i class="fas fa-check-circle"></i></div>
        <div class="stat-value">@completed.Count</div>
        <div class="stat-label" data-t="completed">Completed</div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:#FF980022;color:#FF9800"><i class="fas fa-clock"></i></div>
        <div class="stat-value">@pending.Count</div>
        <div class="stat-label" data-t="pending">Pending</div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:#FF6B6B22;color:#FF6B6B"><i class="fas fa-coins"></i></div>
        <div class="stat-value">@completed.Sum(o => o.Total).ToString("F0") EGP</div>
        <div class="stat-label" data-t="revenue">Revenue</div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <div class="d-flex gap-3 align-items-center flex-wrap">
            <input type="text" class="form-control" style="max-width:220px" placeholder="Search..." oninput="filterTable()">
            <select id="statusFilter" class="form-control" style="max-width:160px" onchange="filterTable()">
                <option value="">All Status</option>
                <option>Pending</option><option>Preparing</option><option>Ready</option><option>Completed</option><option>Cancelled</option>
            </select>
            <select id="typeFilter" class="form-control" style="max-width:160px" onchange="filterTable()">
                <option value="">All Types</option>
                <option>DineIn</option><option>Takeaway</option><option>Delivery</option>
            </select>
        </div>
    </div>
    <div class="table-container">
        <table class="data-table" id="ordersTable">
            <thead>
                <tr>
                    <th data-t="orderNo">Order #</th>
                    <th data-t="cashier">Cashier</th>
                    <th data-t="type">Type</th>
                    <th data-t="items">Items</th>
                    <th data-t="subtotal">Subtotal</th>
                    <th data-t="tax">Tax</th>
                    <th data-t="total">Total</th>
                    <th data-t="payment">Payment</th>
                    <th data-t="status">Status</th>
                    <th data-t="time">Time</th>
                    <th data-t="actions">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var o in Model.OrderByDescending(x => x.CreatedAt))
                {
                    <tr data-status="@o.Status" data-type="@o.OrderType">
                        <td><strong>@o.OrderNumber</strong></td>
                        <td style="font-size:13px">@(o.Cashier?.UserName ?? "-")</td>
                        <td><span class="badge @(o.OrderType == RestaurantERP.Models.OrderType.DineIn ? "badge-primary" : o.OrderType == RestaurantERP.Models.OrderType.Takeaway ? "badge-warning" : "badge-info")">@o.OrderType</span></td>
                        <td>@(o.Items?.Count ?? 0)</td>
                        <td>@o.SubTotal.ToString("F2")</td>
                        <td>@o.TaxAmount.ToString("F2")</td>
                        <td><strong>@o.Total.ToString("F2") EGP</strong></td>
                        <td style="font-size:12px">@o.PaymentMethod</td>
                        <td>
                            <span class="badge @(o.Status==RestaurantERP.Models.OrderStatus.Completed?"badge-success":o.Status==RestaurantERP.Models.OrderStatus.Cancelled?"badge-danger":o.Status==RestaurantERP.Models.OrderStatus.Preparing?"badge-warning":"badge-primary")">
                                @o.Status
                            </span>
                        </td>
                        <td style="font-size:12px">@o.CreatedAt.ToString("MM/dd HH:mm")</td>
                        <td>
                            <a href="/Admin/OrderDetails/@o.Id" class="btn btn-sm btn-outline"><i class="fas fa-eye"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
<script>
function filterTable() {
    const search = document.querySelector('input[type=text]').value.toLowerCase();
    const status = document.getElementById('statusFilter').value;
    const type = document.getElementById('typeFilter').value;
    document.querySelectorAll('#ordersTable tbody tr').forEach(row => {
        const show = (!search || row.textContent.toLowerCase().includes(search)) &&
                     (!status || row.dataset.status === status) &&
                     (!type || row.dataset.type === type);
        row.style.display = show ? '' : 'none';
    });
}
</script>
}
